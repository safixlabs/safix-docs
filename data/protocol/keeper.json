{
  "scripts": {
    "scan": "tsx keeper.ts scan",
    "watch": "tsx keeper.ts watch",
    "drill": "tsx keeper.ts drill",
    "test": "tsx --test src/*.test.ts"
  },
  "dependencies": {
    "viem": "^2.38.0",
    "tsx": "^4.20.0"
  },
  "configFields": [
    "rpcUrl",
    "poolAddress",
    "deployBlock",
    "intervalMs",
    "logChunkBlocks",
    "instanceId",
    "prices",
    "alerts",
    "gas",
    "indexer"
  ],
  "configExample": {
    "rpcUrl": "https://rpc.testnet.chain.robinhood.com",
    "poolAddress": "0x0000000000000000000000000000000000000000",
    "deployBlock": 0,
    "intervalMs": 15000,
    "logChunkBlocks": 50000,
    "instanceId": "keeper-1",
    "prices": {},
    "alerts": {
      "cooldownMs": 900000,
      "stuckScans": 3
    },
    "gas": {
      "warnLiquidations": 200,
      "criticalLiquidations": 50,
      "liquidationGas": 150000
    },
    "indexer": {
      "url": null,
      "timeoutMs": 3000,
      "maxLagBlocks": 5000
    }
  },
  "abi": [
    "function setPrice(address asset, uint256 priceUsd1e18)",
    "function currentPrice(address asset) view returns (uint256 price1e18, uint256 updatedAt)",
    "function isLiquidatable(address borrower, address asset) view returns (bool)",
    "function liquidate(address borrower, address asset, uint256 debtAmount)",
    "function positions(address borrower, address asset) view returns (uint256 collateral, uint256 debt, uint256 principal)",
    "function pausedActions() view returns (uint8)",
    "event Drawn(address indexed borrower, address indexed asset, uint256 amount, uint256 fee)"
  ],
  "defaultIntervalMs": 0
}
